{
  "buildCommand": "cd distributed-node && rustup target add wasm32-unknown-unknown && cargo install wasm-pack && wasm-pack build --target web --out-dir ../public/pkg",
  "outputDirectory": "public",
  "framework": null,
  "git": {
    "deploymentEnabled": {
      "feature/distributed-node": true
    }
  }
}
