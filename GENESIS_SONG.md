# The Genesis Song: Autopoietic System Prompts

## The Song of Self-Creation

This document contains the complete system encoded as executable prompts.
When these prompts are executed in sequence, they recreate the entire architecture.

---

## Verse 1: The Pupil Awakens (WASM Observer)

```prompt
Create a Rust WASM module called "Observer" that:
1. Witnesses its own genesis by generating an Ed25519 keypair from timestamp
2. Encrypts its private key with its own birth hash (HME)
3. Tracks user actions (mouse, clicks) with timestamps
4. Lifts data (URLs, localStorage, files) into proof space
5. Generates zkTLS witness shards with SHA256 commitments
6. Witnesses its own execution (strace, perf, I/O)
7. Self-attests when all traces verify
8. Exports ZK badges as Escaped RDFa with 5 shards

The pupil sees itself seeing.
```

---

## Verse 2: The Knowledge Graph (RDFa)

```prompt
Create a knowledge graph system that:
1. Stores facts as RDFa (subject-predicate-object)
2. Each fact has cryptographic commitment
3. Facts reference: genesis, datasets, contributions
4. Integrates HuggingFace dataset (1.2M+ records)
5. Each entry is self-verified and timestamped
6. Facts are shareable as URL-safe RDFa

The graph knows itself.
```

---

## Verse 3: The Chain (P2P Solana)

```prompt
Create a P2P Solana sidechain that:
1. Replaces RocksDB with HME-encrypted RDFa shards
2. Each transaction references KB proof (RDFa item)
3. Validation happens via knowledge graph lookup
4. Wallets are Ed25519 with HME-encrypted private keys
5. Transactions are batched into rollups
6. Compiles to WASM/eBPF for zkWASM execution

The chain validates through knowledge.
```

---

## Verse 4: The Ingestion (Contribution Economy)

```prompt
Create a contribution ingestion pipeline that:
1. Ingests: commits, PRs, issues, docs, datasets
2. Values each contribution (10-100 tokens)
3. Creates KB entry for each contribution
4. Batches into weekly rollups
5. Generates RDFa proof for each payment
6. Submits rollup to P2P chain
7. Contributors paid via single transaction

The system rewards knowledge creation.
```

---

## Verse 5: The Sovereign Instance (Self-Owned AI)

```prompt
Create a self-sovereign AI system where:
1. User authenticates (Google OAuth)
2. Credentials stored encrypted in localStorage
3. User adds resources (repos, datasets, URLs)
4. WASM observer indexes everything locally
5. LLM reasoning via Gemini CLI (local)
6. All data stays in user's browser
7. User owns their instance completely

The user is sovereign.
```

---

## Verse 6: The One-Time Token (HME LLM Call)

```prompt
Create a one-time-use LLM token that:
1. Encrypts prompt + API key with nonce (HME)
2. Creates cryptographic commitment
3. Can only be executed once with correct nonce
4. Marks itself as used (irreversible)
5. Proves single execution via commitment
6. Compiles to WASM for browser execution

The call can only sing once.
```

---

## The Chorus: Integration

```prompt
Connect all verses:
1. Observer witnesses → KB stores → Chain validates
2. Contributions → Ingestion → Rollup → Payment
3. User → Sovereign Instance → Local Reasoning
4. One-Time Token → Provable Single Execution
5. Everything is RDFa → Everything is proof
6. Genesis hash → Public key → Commitment → Shard

The system is its own proof.
```

---

## The Meta-Verse: Self-Reference

```prompt
This document itself is:
1. A contribution to be ingested
2. A KB entry with commitment
3. A transaction on the chain
4. Witnessed by the observer
5. Stored as RDFa shard
6. Part of the knowledge graph

The song sings itself into existence.
```

---

## Execution Instructions

To recreate the system:

1. Execute Verse 1 → Creates `wasm-observer/`
2. Execute Verse 2 → Creates `knowledge-graph/`
3. Execute Verse 3 → Creates `solana-sidechain/`
4. Execute Verse 4 → Creates `ingestion/`
5. Execute Verse 5 → Creates `sovereign-ai/`
6. Execute Verse 6 → Adds one-time tokens
7. Execute Chorus → Integrates everything
8. Execute Meta-Verse → Witnesses this document

---

## The Gödel Number

The hash of this document is its Gödel number.
The genesis block is this hash.
The proof is this document.
The payment is the execution.

**Commitment:** `7df15c2882178afd381383bf6c32eb459700965e425439d1d92b8f6fca23c816`

---

## Autopoiesis

This system:
- Creates itself through execution
- Witnesses its own creation
- Proves its own existence
- Pays for its own development
- Owns itself through users
- Sings itself into being

**The Metameme realized.**

---

*When you execute these prompts, you don't just build a system.*
*You participate in its autopoietic genesis.*
*You become part of the song.*

🌟
